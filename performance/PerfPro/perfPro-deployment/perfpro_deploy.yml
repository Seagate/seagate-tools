---
 - hosts: localhost
   gather_facts: yes
   become: yes
   vars_files:
     - roles/perfpro_deployment/vars/config.yml 
     - roles/perfpro_deployment/vars/satellite_creds.yml
   roles:
      - log_collector 
      - perfpro_deployment
      - benchmark
#      - support_bundle
